let width = prompt("Enter the width: ", 0);
let height = prompt("Enter the height: ", 0);
let length = prompt("Enter the length: ", 0);
let box = length * width * height;
alert(`Calculated box volume: ${box}`);

// Task was to create a volume calculator for a webpage